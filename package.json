{
  "name": "latchpoint",
  "version": "1.0.0",
  "private": true,
  "description": "Pre-commitment financial risk intelligence system",
  "scripts": {
    "dev": "concurrently -k -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && ((test -f venv/bin/uvicorn && ./venv/bin/uvicorn app.main:app --reload --port 8000) || (test -f venv/Scripts/uvicorn.exe && venv/Scripts/uvicorn app.main:app --reload --port 8000) || uvicorn app.main:app --reload --port 8000)",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "lint:frontend": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
